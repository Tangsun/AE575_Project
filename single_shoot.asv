function e = single_shoot(p_0)
    global x0 nx T
    
    p = p_0
    for i = 1: length(T)-1
        cost_i = zeros(3, 1);
        for j = 1: 3
            cost_i(i) = p'*
        end
    end
end